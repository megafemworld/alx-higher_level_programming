#!/usr/bin/python3
"""function that finds a peak in a list of unsorted integers."""

def find_peak(list_of_integers):
    """find the peak of a list"""
    if (len(list_of_integer)) < 3:
        return None
