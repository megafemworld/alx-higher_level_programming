#!/usr/bin/python3
"""lookup function that returns attributes & methods of an object
    argument:
    obj: the object to check
"""


def lookup(obj):
    """lookup defination"""
    return (dir(obj))
